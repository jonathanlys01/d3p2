# Default configurations for experiments on JZ

# Paths
mdlm_model_path: ${env_path_or:WORK,models/kuleshov-group/mdlm-owt/,/Brain/public/models/kuleshov-group/mdlm-owt/}
mdlm_tokenizer: ${env_path_or:DSDIR,HuggingFace_Models/gpt2/,gpt2}

ppl_model_id: ${env_path_or:DSDIR,HuggingFace_Models/gpt2/,gpt2}
cos_model_id: ${env_path_or:WORK,models/jinaai/jina-embeddings-v2-base-en/,jinaai/jina-embeddings-v2-base-en}

seed: 42
cache_dir: ./.cache
n_runs: 4
compile_model: true
num_steps: 1024
cat_temperature: 1.0

n_groups: 4
group_size: 4

split_groups: true
dpp: true
w_interaction: 1.0
w_split: 0.0
subsample_start: 300
subsample_end: 400